# The Queue

## Viewing the Queue

Seeing the contents of the queue is as easy as running the `/queue` command. You will then be shown a paginated list of your queue. To traverse the queue, all you have to do is click the buttons.

![Example](https://i.imgur.com/EJs6llL.gif)